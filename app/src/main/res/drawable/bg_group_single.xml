<?xml version="1.0" encoding="utf-8"?>
<!--
    SPDX-FileCopyrightText: 2026 Patrick Gaskin
    SPDX-License-Identifier: GPL-3.0-or-later

    A row that is a group of one: every corner large. The other three are the
    same drawable with a different pair of ends — see RowGroups, which is what
    chooses between them.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">

    <item>
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorSurfaceBright" />
            <corners android:radius="@dimen/group_radius" />
        </shape>
    </item>

    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="@dimen/group_radius" />
        </shape>
    </item>

</ripple>
